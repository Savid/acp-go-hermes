# Permissions

Hermes `approval.request` gateway events are mapped to ACP
`session/request_permission`. ACP replies are sent back through native
`approval.respond` with `once`, `always`, or `deny`.

On cancellation, pending native permission requests are rejected with a
cancelled explanation and the ACP prompt returns `cancelled`.
