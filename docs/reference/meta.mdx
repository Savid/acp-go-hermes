# Metadata

Lifecycle requests accept:

```json
{
  "_meta": {
    "hermes": {
      "options": {
        "model": "provider/model",
        "env": {},
        "mode": "build",
        "permission": "ask"
      },
      "rawEvent": {
        "enabled": false
      }
    }
  }
}
```

Unknown keys under `_meta.hermes` are rejected. Foreign namespaces are
ignored.

`_meta.hermes.options.permission` accepts `ask` or `allow`. Empty is treated
as `ask`. The adapter writes the value into the isolated per-session Hermes
config as `permission: {"*": "<value>"}` before starting `hermes serve`.
